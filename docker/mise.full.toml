# full 镜像全局默认工具链（mise 管理）= core + Java。
# Java 默认 temurin-17；需要 Java 8 的用户只需设置
#   MISE_JAVA_VERSION: "temurin-8"
# 首次启动会自动补装（懒加载），补装的版本持久化在 /opt/mise 卷上。
[tools]
go = "1.25.12"
node = "22.19.0"
"npm:pnpm" = "10.32.1"
"npm:bun" = "1.3.14"
python = "3.12"
java = "temurin-17"
maven = "3.9"
